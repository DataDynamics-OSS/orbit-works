# Operator secrets (passwords, tokens) — never commit
ops/secrets.local
prod-admin-credentials.txt
# Runtime configuration — derived from *.template, contains secrets / host-specific edits
config.yaml
docker-compose.yml

# OSS 공개 레포에 포함하지 않는 경로 (내부 운영용)
docs/
mobile/
scripts/
ops/
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

# Claude Code local settings
.claude/

# Env
.env
.env.local
.env.production

# IDE
.vscode/
.idea/
*.swp

# Uploads / data
data/
/data/
uploads/
# 애플리케이션 시드/리소스 데이터 (사용자 업로드 X) — 추적해야 함
!backend/app/data/
!backend/app/data/**

# PostgreSQL 데이터 디렉터리 — docker-compose.yml 의
# ${PG_DATA_DIR:-./pgdata} 가 레포 안에 bind-mount 로 생성한다.
# (권한 0700 / postgres uid 소유라 git 이 읽지도 못하고, 수백 MB 규모)
pgdata/
/pgdata/

# Backups
backups/

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db
